[pytest]
testpaths = modules/orchestration/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts =
    -v
    --tb=short
    --strict-markers
    --cov=modules.orchestration
    --cov-report=term-missing
    --cov-report=html
